

<h1 class="mb-1 restaurant-show-title">{{restaurant.name}}</h1>
<div class="container">
  <div class="row mb-4">
    <div class="col-12 col-md-10 mx-auto position-relative">

      <div class="position-absolute" style="right:0px;">
        <a class="btn btn-outline-secondary btn-sm" href="/restaurants/{{restaurant._id}}/edit" role="button"><i class="fas fa-edit"></i></a>
        <form action="/restaurants/{{restaurant._id}}/delete" method="POST" onsubmit="return webConfirm('你確定要刪除這間餐廳嗎？')" style="display: inline;">
          <button class="btn btn-outline-danger btn-sm" type="submit">X</button>
        </form>
      </div>

      <p class="mb-1">
        <span class="text-secondary">
          <i class="fas fa-utensils pr-2"></i>
          類別：
        </span>
        {{restaurant.category}}
      </p>

      <p class="mb-1">
        <span class="text-secondary">
          <i class="fas fa-map-marker-alt pr-2"></i>
          地址：
        </span>
        {{restaurant.location}}
        <a href="{{restaurant.google_map}}" class="text-secondary" target="_blank">
          <i class="fas fa-location-arrow pr-2 fa-xs"></i>
        </a>
      </p>

      <p class="mb-1">
        <span class="text-secondary">
          <i class="fas fa-mobile-alt pr-2"></i>
          電話：
        </span>
        {{restaurant.phone}}
      </p>

      <p class="mb-5">
        {{restaurant.description}}
      </p>

      <img class="rounded mx-auto d-block mb-4 w-100" src="{{restaurant.image}}" alt="{{restaurant.name}}" style="max-width: 600px;">
    </div>
    <div class="col-12 col-md-10 mx-auto text-center">
      <a class="btn btn-outline-secondary" href="/" role="button">回到首頁</a>
    </div>
  </div>
</div>